---

name: lint

on: push

jobs:

  build:
    runs-on: ubuntu-22.04

    steps:
      - uses: actions/checkout@v4

      - name: Install Dependencies
        run: make install

      - name: Run Linter
        run: make lint
